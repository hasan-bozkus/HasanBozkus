@using EntityLayer.Concrete
@model List<AppUser>


<div class="card">
    <div class="card-body">
        <div class="d-flex flex-column align-items-center text-center">
            @foreach (var item in Model)
            {
                <img src="~/WriterImageFiles/@item.ImageUrl" alt="Admin" class="rounded-circle" width="150">
                <div class="mt-3">
                    <h4>@item.NameSurname</h4>
                    <h5 class="mb-0">Kullanıcı Adı  : @ViewBag.userName </h5>
                    <p class="text-secondary mb-1">@item.Email</p>
                    <p class="text-muted font-size-sm">@item.PhoneNumber</p>

                    <a href="/Message/Inbox/" class="btn btn-outline-primary">Message Kutum</a>
                </div>
            }
        </div>
    </div>
</div>